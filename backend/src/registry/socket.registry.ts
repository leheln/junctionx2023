import {Socket} from 'socket.io';

// Register socket based features into this file
export const socketRegistry = async (socket: Socket): Promise<void> => {
    // await exampleSocketFeature(socket);
}
